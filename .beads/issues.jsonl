{"id":"auxide-midi-63d","title":"Definition of Done","description":"- implemented","status":"tombstone","priority":2,"issue_type":"task","created_at":"2026-07-24T15:15:54.6574833-05:00","updated_at":"2026-07-24T15:16:39.9243284-05:00","deleted_at":"2026-07-24T15:16:39.9243284-05:00","deleted_by":"batch delete","delete_reason":"batch delete","original_type":"task"}
{"id":"auxide-midi-8la","title":"Make MIDI synth path actually playable","description":"Make the MIDI synth path actually playable (bug). The current bridge computes state but produces no sound because it is not wired to a runtime (see midi-u4h.1). This bead adds the musician-friendly `Synth` facade (`note_on`/`note_off`/`process`) that wraps MidiToAudioBridge + RuntimeCore, so a user (or headless test) can play notes and verify audio. This is the user-visible \"it works\" gate.\n\n## Steps to Reproduce\n1. Build a graph with MidiToAudioBridge and feed NoteOn(69) via bridge.poll().\n2. Render the RuntimeCore output blocks.\n3. Observe output is silence — the bridge never sent a ControlMsg to the runtime.\n\n## Acceptance Criteria\n1. `cargo test -p auxide-midi` green; clippy clean.\n2. NEW public `Synth` with `new(spec) -\u003e (Self, RuntimeControl)`, `note_on(note, velocity)`, `note_off(note)`, `process_block(\u0026mut [f32])` calling RuntimeCore.process_block_with_channels.\n3. NEW `synth_plays`: note_on(69,100) → non-zero; after note_off + enough blocks → ~0.\n4. NEW `synth_polyphony`: 8 simultaneous note_on → 8 active voices; 9th steals oldest (no panic).\n5. Synth is drivable programmatically with no MIDI device (headless tests).\n\n## Design\nA. `pub struct Synth { core, control, bridge, node_map }`.\nB. `new` builds poly graph, RuntimeCore::new_with_channels, bridge with control+node_map.\nC. note_on/off delegate to bridge; process_block calls core.process_block_with_channels.\nD. Add `#[cfg(test)] synth_plays` / `synth_polyphony`. Iterate.","design":"PLAN:\nA. Add `pub struct Synth { core: RuntimeCore, control: RuntimeControl, bridge: MidiToAudioBridge, node_map }` (or split control out).\nB. `new` builds the poly graph (reuse the helper from midi-u4h.1), calls RuntimeCore::new_with_channels, constructs the bridge with the control + node_map.\nC. `note_on/note_off` delegate to bridge.poll-equivalent sending ControlMsg; `process_block` calls `core.process_block_with_channels(\u0026mut out, \u0026mut control.control_rx, \u0026mut control.invariant_tx)` (or the RuntimeHandle::process_block if core+channels are bundled).\nD. Add the `#[cfg(test)]` `synth_plays` / `synth_polyphony`. Iterate until green.","acceptance_criteria":"GATED EXIT — all must hold:\n1. `cargo test -p auxide-midi` green; `cargo clippy -p auxide-midi -- -D warnings` clean.\n2. NEW public `Synth` (or `MidiSynth`) type with `new(spec) -\u003e (Self, RuntimeControl)` (or self-containing), `note_on(note:u8, velocity:u8)`, `note_off(note:u8)`, and `process_block(\u0026mut [f32])` that calls RuntimeCore.process_block_with_channels.\n3. NEW test `synth_plays`: `s.note_on(69,100)` then process_block → output non-zero; after `s.note_off(69)` and enough blocks → output ≈0 (release complete).\n4. NEW test `synth_polyphony`: 8 simultaneous note_on → 8 active voices (via bridge.active_voice_count()); a 9th steals oldest (still audible, no panic).\n5. The Synth is drivable programmatically with no MIDI device (so tests are headless).","status":"open","priority":1,"issue_type":"bug","owner":"michaelallenkuykendall@gmail.com","created_at":"2026-03-06T23:28:18.1362132-06:00","created_by":"Michael A. Kuykendall","updated_at":"2026-07-24T18:05:00.4815189-05:00","labels":["midi","repair","suite","synth"],"dependencies":[{"issue_id":"auxide-midi-8la","depends_on_id":"midi-u4h.1","type":"blocks","created_at":"2026-07-24T15:26:01.4402459-05:00","created_by":"Michael A. Kuykendall"}]}
{"id":"auxide-midi-b0f","title":"Definition of Done","description":"- alpha done","status":"tombstone","priority":2,"issue_type":"task","created_at":"2026-07-24T15:16:10.3540323-05:00","updated_at":"2026-07-24T15:16:39.6440991-05:00","deleted_at":"2026-07-24T15:16:39.6440991-05:00","deleted_by":"batch delete","delete_reason":"batch delete","original_type":"task"}
{"id":"auxide-midi-bgq","title":"Add MIDI clock and transport parsing (start/stop/continue, SPP)","description":"Add MIDI clock and transport parsing: start (0xFA), stop (0xFB), continue (0xFC), real-time clock (0xF8, 24 ppq), and Song Position Pointer (0xF2). Today midi_input.rs parses Note/CC/PitchBend only. Add the events plus a `Transport` tracker that derives beat/bar from clock ticks.\n\n## Acceptance Criteria\n1. `cargo test -p auxide-midi` green; clippy clean.\n2. NEW MidiEvent variants: `Clock`, `Start`, `Stop`, `Continue`, `SongPosition(u16)` parsed from raw status bytes.\n3. NEW `parse_transport`: feed 0xFA, 0xF8 x3, 0xFC → Start, Clock, Clock, Clock, Stop; feed 0xF2 0x08 0x01 → SongPosition(0x108).\n4. NEW `Transport` counting clocks (24/quarter): after 96 clocks beat==4 and bar advances; Start resets; Stop holds; Continue resumes.\n\n## Design\nA. Extend midi_input.rs parser for 0xF8..0xFC and 0xF2 (3-byte).\nB. Add `pub struct Transport { clocks, running }` with tick/start/stop/cont/song_position + getters beat()/bar()/ppq_phase().\nC. Add `#[cfg(test)]` parse + Transport tests. Iterate.","design":"PLAN:\nA. Extend midi_input.rs parser: match 0xF8..0xFC and 0xF2 (3-byte) → new MidiEvent variants.\nB. Add `pub struct Transport { clocks: u64, running: bool }` with `fn tick()`, `fn start()`, `fn stop()`, `fn cont()`, `fn song_position(u16)`, and getters `beat()`, `bar()`, `ppq_phase()`.\nC. Add the `#[cfg(test)]` parse + Transport tests. Iterate until green.","acceptance_criteria":"GATED EXIT — all must hold:\n1. `cargo test -p auxide-midi` green; `cargo clippy -p auxide-midi -- -D warnings` clean.\n2. NEW MidiEvent variants: `Clock`, `Start`, `Stop`, `Continue`, `SongPosition(u16)` parsed from raw status bytes in midi_input.rs.\n3. NEW test `parse_transport`: feed bytes 0xFA, 0xF8 x3, 0xFC → events Start, Clock, Clock, Clock, Stop; feed 0xF2 0x08 0x01 → SongPosition(0x108).\n4. NEW `Transport` struct counting clocks (24/quarter): after 96 clocks assert beat==4 and bar advances; Start resets to 0; Stop holds; Continue resumes.","status":"open","priority":2,"issue_type":"task","owner":"michaelallenkuykendall@gmail.com","created_at":"2026-07-24T15:19:56.5454731-05:00","created_by":"Michael A. Kuykendall","updated_at":"2026-07-24T18:05:29.4446303-05:00","labels":["midi","pts-5"],"dependencies":[{"issue_id":"auxide-midi-bgq","depends_on_id":"auxide-midi-8la","type":"blocks","created_at":"2026-07-24T15:26:01.5917849-05:00","created_by":"Michael A. Kuykendall"}]}
{"id":"auxide-midi-drc","title":"Acceptance Criteria","description":"- [ ] criterion one","status":"tombstone","priority":2,"issue_type":"task","created_at":"2026-07-24T15:15:54.6559068-05:00","updated_at":"2026-07-24T15:16:40.0629354-05:00","deleted_at":"2026-07-24T15:16:40.0629354-05:00","deleted_by":"batch delete","delete_reason":"batch delete","original_type":"task"}
{"id":"auxide-midi-eis","title":"Acceptance Criteria","description":"- [ ] beta criterion","status":"tombstone","priority":2,"issue_type":"task","created_at":"2026-07-24T15:16:10.3555846-05:00","updated_at":"2026-07-24T15:16:39.7821496-05:00","deleted_at":"2026-07-24T15:16:39.7821496-05:00","deleted_by":"batch delete","delete_reason":"batch delete","original_type":"task"}
{"id":"auxide-midi-qi2","title":"Stand up one honest MicroFreak-first reference synth","description":"Stand up one honest MicroFreak-first reference ROMPLER (sampler-based reference synth): a complete, runnable example that plugs a MIDI controller in and plays polyphonically through the whole stack (auxide-dsp Sampler voices → MidiToAudioBridge → RuntimeCore → auxide-io StreamController), using a RECORDED timbre (a .wav you supply, or a generated sample) instead of from-scratch oscillator synthesis. Filter cutoff/resonance on CC and pitch bend match the Arturia MicroFreak defaults. This is the simplest possible \"plug in and play\" path: one recording propagates across the keyboard via pitch-shifting.\n\n## Acceptance Criteria\n1. `cargo build --example microfreak_synth` AND `cargo build --example poly_synth` succeed; clippy clean.\n2. The example builds N Sampler voices (from a loaded .wav or generated sample) → optional SvfFilter → mixer → OutputSink via the Synth facade, wired to `StreamController::play_handle` + midir.\n3. CC mappings match MicroFreak defaults (CC74=cutoff, CC71=resonance; pitch bend ±2 semitones), documented in the example header.\n4. A unit test proves the graph the example builds renders non-zero audio via the same runtime path as auxide-midi-8la.\n5. README links this example as the reference synth (now a ROMpler, not a subtractive synth).\n\n## Design\nA. Write `examples/microfreak_synth.rs`: Sampler voices (one recorded timbre, pitch-shifted per note) + optional SVF + AdsrEnvelope amplitude + mixer + reverb; MicroFreak CC mappings; midir→bridge; StreamController::play_handle.\nB. Update `examples/poly_synth.rs` to use the Synth facade (Sampler voices).\nC. Add documented CC map header; test builds same graph headlessly, assert non-zero audio. Iterate.","design":"PLAN:\nA. Write `examples/microfreak_synth.rs`: construct Synth (osc=SawOsc/SquareOsc, filter=SvfFilter, env=AdsrEnvelope, mixer, reverb), set cc_mappings to MicroFreak defaults, connect midir → bridge.poll loop, StreamController::play_handle(runtime_handle).\nB. Update `examples/poly_synth.rs` to use the same Synth facade (not the old disconnected bridge).\nC. Add the documented CC map header; add a test that builds the same graph headlessly and asserts non-zero audio.\nD. Iterate until builds + tests green.","acceptance_criteria":"GATED EXIT — all must hold:\n1. `cargo build --example microfreak_synth` AND `cargo build --example poly_synth` succeed; `cargo clippy -p auxide-midi -- -D warnings` clean.\n2. The example builds a polyphonic graph (osc + SVF/Ladder filter + ADSR env + optional reverb) via the Synth facade from auxide-midi-8la and wires it to `StreamController::play_handle` + a real MIDI device (midir).\n3. CC mappings match MicroFreak defaults (e.g., CC74=cutoff, CC71=resonance; pitch bend ±2 semitones) and are documented in the example's header comment.\n4. A unit test (or doc) proves the graph the example builds renders non-zero audio through the same runtime path as auxide-midi-8la (reuse `synth_plays` style assertion).\n5. README links this example as the reference synth.","status":"open","priority":2,"issue_type":"task","owner":"michaelallenkuykendall@gmail.com","created_at":"2026-07-24T15:19:56.7482128-05:00","created_by":"Michael A. Kuykendall","updated_at":"2026-07-24T18:52:20.5456384-05:00","labels":["midi","pts-8"],"dependencies":[{"issue_id":"auxide-midi-qi2","depends_on_id":"auxide-midi-bgq","type":"blocks","created_at":"2026-07-24T15:25:07.5087801-05:00","created_by":"Michael A. Kuykendall"}]}
{"id":"auxide-midi-y72","title":"Acceptance Criteria","description":"- [ ] alpha criterion","status":"tombstone","priority":2,"issue_type":"task","created_at":"2026-07-24T15:16:10.3529988-05:00","updated_at":"2026-07-24T15:16:39.5072097-05:00","deleted_at":"2026-07-24T15:16:39.5072097-05:00","deleted_by":"batch delete","delete_reason":"batch delete","original_type":"task"}
{"id":"auxide-midi-yk7","title":"Make MIDI demos and docs match actual capability","description":"Make MIDI demos and docs match actual capability. Before midi-u4h.1/8la, the examples and README implied a playable synth that did not produce sound. After the runtime path is wired, rewrite examples (poly_synth, note_echo, list_devices) and the README quick-start to use the real Synth facade, real CC mappings, and pitch bend — no overclaims.\n\n## Acceptance Criteria\n1. `cargo test -p auxide-midi` green; `cargo build --examples` succeeds; clippy clean.\n2. Every example compiles and uses the real Synth/`note_on`/`note_off` API (no old disconnected bridge-only flow).\n3. README quick-start is a doctest or references a building example and compiles; states true capability (polyphonic, CC cutoff/res, pitch bend) and notes device requirement.\n4. No remaining doc/example claim that the synth is playable without the wiring that now exists.\n\n## Design\nA. Rewrite poly_synth.rs and note_echo.rs to use the Synth facade + real CC mappings; list_devices.rs stays device-listing.\nB. Rewrite README quick-start to real API; add `rust,no_run` doctest or point to building example.\nC. Remove overclaims; align with auxide-midi-qi2. Run build/tests/clippy. Iterate.","design":"PLAN:\nA. Rewrite `examples/poly_synth.rs` and `examples/note_echo.rs` to use the Synth facade (auxide-midi-8la) + real CC mappings; `list_devices.rs` can stay device-listing only.\nB. Rewrite README quick-start to the real API; add `# ```rust,no_run` doctest or point to the building example.\nC. Remove overclaims; align with auxide-midi-qi2 reference.\nD. Run `cargo build --examples` + `cargo test` + clippy; iterate.","acceptance_criteria":"GATED EXIT — all must hold:\n1. `cargo test -p auxide-midi` green; `cargo build --examples` succeeds; `cargo clippy -p auxide-midi -- -D warnings` clean.\n2. Every example file compiles and uses the real Synth/`note_on`/`note_off` API (no references to the old disconnected bridge-only flow).\n3. README quick-start is a doctest or references a building example and compiles; it states the true capability (polyphonic, CC cutoff/res, pitch bend) and notes device requirement.\n4. No remaining doc/example claim that the synth is playable without the wiring that now exists.","status":"open","priority":2,"issue_type":"task","owner":"michaelallenkuykendall@gmail.com","created_at":"2026-03-06T23:28:18.9649446-06:00","created_by":"Michael A. Kuykendall","updated_at":"2026-07-24T18:05:29.6838122-05:00","labels":["docs","midi","repair"]}
{"id":"midi-u4h","title":"Private planning bootstrap","description":"P0 planning/bootstrap epic for auxide-midi. Owns the make-it-playable work: the runtime-controlled MIDI→audio path (midi-u4h.1), making that path actually audible (auxide-midi-8la), the MicroFreak reference synth (auxide-midi-qi2), MIDI clock/transport (auxide-midi-bgq), and docs parity (auxide-midi-yk7). Highest-priority epic — its P0 children must be completable first. Closes only after all children have populated acceptance + design and the synth produces sound end-to-end.\n\n## Success Criteria\n- Every child (midi-u4h.1, auxide-midi-8la/qi2/bgq/yk7) has non-empty acceptance and design.\n- The runtime-controlled path is green: `cargo test -p auxide-midi` passes AND an integration test proves NoteOn/NoteOff drives the auxide RuntimeCore to produce (then silence) audio.\n- `cargo clippy -p auxide-midi -- -D warnings` clean.","design":"PLAN: Tracker only (P0). Drive children to closure; verify the end-to-end audible test; close last. Sequence: midi-u4h.1 (wire ControlMsg) → auxide-midi-8la (Synth facade) → auxide-midi-qi2 (reference) → auxide-midi-bgq → auxide-midi-yk7 (docs).","acceptance_criteria":"GATED EXIT — epic closes only when:\n1. Every child (midi-u4h.1, auxide-midi-8la/qi2/bgq/yk7) has non-empty `--acceptance` and `--design`.\n2. The runtime-controlled path is green: `cargo test -p auxide-midi` passes AND an integration test proves NoteOn/NoteOff drives the auxide RuntimeCore to produce (then silence) audio.\n3. `cargo clippy -p auxide-midi -- -D warnings` clean.","status":"open","priority":0,"issue_type":"epic","owner":"michaelallenkuykendall@gmail.com","created_at":"2026-03-06T17:38:00.222435-06:00","created_by":"Michael A. Kuykendall","updated_at":"2026-07-24T18:04:59.8647952-05:00"}
{"id":"midi-u4h.1","title":"Build the runtime-controlled MIDI synth path","description":"Build the runtime-controlled MIDI synth path. Today MidiToAudioBridge (src/midi_bridge.rs) maintains a VoiceAllocator + VoicePool and computes smoothed CC values via get_parameter(), but it is NOT connected to the auxide kernel: it never emits ControlMsg into a RuntimeControl, so nothing drives the RuntimeCore and no audio is produced. This bead wires bridge → RuntimeControl → RuntimeCore so MIDI events actually articulate DSP nodes.\n\n## Acceptance Criteria\n1. `cargo test -p auxide-midi` green; clippy clean.\n2. MidiToAudioBridge gains a constructor/setter accepting a `RuntimeControl` plus a `voice_node_map: Vec\u003c(osc_node, env_node, filter_node)\u003e` from a polyphonic graph.\n3. On NoteOn: send SetFrequency(osc, note_to_freq) + TriggerGate(env, true) (+ velocity gain); NoteOff: TriggerGate(env,false). On CC: SetFilterCutoff/SetFilterResonance. On PitchBend: SetFrequency adjust.\n4. NEW `midi_drives_runtime`: build poly graph → RuntimeCore → bridge with control+node map → NoteOn(69) → non-zero audio AND ~440 Hz; NoteOff → decays to ~0.\n5. NEW `cc_moves_cutoff`: CC74 changes audible filter cutoff (spectrum centroid shifts).\n\n## Design\nA. Add RuntimeControl field + voice_node_map to bridge; `with_runtime(control, node_map)`.\nB. In poll(), after allocator/CC logic, translate to ControlMsg via control.send(...); reuse note_to_freq.\nC. Build poly graph in auxide-dsp helper returning (Graph, Plan, voice_node_map), 8 voices.\nD. Add integration tests driving RuntimeCore headless. Iterate.","design":"PLAN:\nA. Add `RuntimeControl` field + `voice_node_map` to MidiToAudioBridge; add `with_runtime(control, node_map)` constructor.\nB. In `poll()`, after the existing allocator/CC logic, translate to ControlMsg via `self.control.as_mut().unwrap().send(...)`. Reuse auxide_midi::conversions::note_to_freq.\nC. Build the polyphonic graph either in auxide-dsp (a helper returning (Graph, Plan, voice_node_map)) or in a midi test helper; 8 voices (match VoicePool MAX_VOICES=8).\nD. Add the `#[cfg(test)]` integration tests driving RuntimeCore directly (headless, no device). Iterate until green.","acceptance_criteria":"GATED EXIT — all must hold:\n1. `cargo test -p auxide-midi` green; `cargo clippy -p auxide-midi -- -D warnings` clean.\n2. MidiToAudioBridge gains a constructor/setter accepting a `RuntimeControl` (auxide::rt::RuntimeControl) plus a `voice_node_map: Vec\u003c(osc_node, env_node, filter_node)\u003e` (NodeId plan from a polyphonic graph built in auxide-dsp/auxide).\n3. On NoteOn(note,vel): allocate voice → send ControlMsg::SetFrequency(osc_node, note_to_freq(note)) and ControlMsg::TriggerGate(env_node, true) (and a velocity gain via SetGain/SetParam); on NoteOff: TriggerGate(env_node,false) (or AllNotesOff on voice steal). On CC (per cc_mappings): SetFilterCutoff/SetFilterResonance. On PitchBend: SetFrequency adjust.\n4. NEW integration test `midi_drives_runtime`: build a poly graph (N osc→filter→env→mixer→sink in auxide-dsp) → RuntimeCore::new_with_channels → bridge with that control + node map → send NoteOn(69) → run blocks → assert non-zero audio AND fundamental ~440 Hz; send NoteOff → assert decays to ~0 within release.\n5. NEW test `cc_moves_cutoff`: CC74 changes the audible filter cutoff (assert spectrum centroid shifts).","status":"open","priority":0,"issue_type":"task","owner":"michaelallenkuykendall@gmail.com","created_at":"2026-03-06T17:38:14.6093438-06:00","created_by":"Michael A. Kuykendall","updated_at":"2026-07-24T18:05:00.1181533-05:00","dependencies":[{"issue_id":"midi-u4h.1","depends_on_id":"midi-u4h","type":"parent-child","created_at":"2026-03-06T17:38:14.6109196-06:00","created_by":"Michael A. Kuykendall"}]}
